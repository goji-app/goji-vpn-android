<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Активность полностью на Compose (ComponentActivity + setContent), поэтому
         XML-тема не обязана быть Material3 — используем системную, чтобы не тащить
         отдельную зависимость com.google.android.material:material только ради неё. -->
    <style name="Theme.GodjiVpn" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>
</resources>
